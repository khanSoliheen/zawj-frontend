# Frontend environment example
#
# Only EXPO_PUBLIC_* values belong in the Expo client bundle.
# Do not put service-role keys, database passwords, or backend-only secrets here.
# Replace values locally as needed.

EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
