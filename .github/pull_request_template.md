## Summary

- what changed
- why it changed

## Scope

- affected routes:
- affected shared modules:
- auth/session impact:
- backend or Supabase impact:

## Testing

- [ ] `pnpm exec eslint app src test --ext .js,.ts,.tsx --max-warnings=0`
- [ ] `pnpm exec tsc --noEmit`
- [ ] `pnpm exec jest --runInBand --passWithNoTests=false`
- [ ] `pnpm test:coverage` if auth, routing, registration, chat, settings, or shared state changed

## Risk

- user-facing regression risk:
- fallback or rollback note:

## Notes

- `PLANS.md` used for route/auth/state/native-risk work:
- new dependency or Expo config change requiring approval:
- follow-up work:
